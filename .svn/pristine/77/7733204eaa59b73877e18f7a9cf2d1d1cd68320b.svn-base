package br.com.speedy.wsipapp;

import javax.ws.rs.GET;
import javax.ws.rs.Path;
import javax.ws.rs.Produces;
import javax.ws.rs.core.MediaType;

@Path("/myresourcelogin")
public class MyResourceLogin {

	
	@GET
	@Produces(MediaType.APPLICATION_JSON)
	@Path("/logar")
	public Boolean logar() {
		/*JsonObject object = null;
		String login = object.getString("login");
		String senha = object.getString("senha");
		
		EntityManager manager = JpaUtil.getEntityManager();
		UsuarioRepository usuarios = new UsuarioRepository(manager);
		Boolean response = usuarios.getUsuario(login, senha) != null ? true : false;
		*/
		return true;
	}
	
}
